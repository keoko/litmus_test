<h2>Edit interview</h2>

<%= render "form.html", changeset: @changeset,
                        action: interview_path(@conn, :update, @interview) %>

<%= link "Back", to: interview_path(@conn, :index) %>
